[tool.pyright]
include = ["src", "tests"]
exclude = [
    "**/__pycache__",
    "**/.worktrees/**",
    ".venv/**",
    ".superpowers/**",
]
pythonVersion = "3.12"
typeCheckingMode = "standard"

[tool.pytest.ini_options]
testpaths = ["tests"]
